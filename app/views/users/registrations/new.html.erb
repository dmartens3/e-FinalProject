<div class="max-w-md mx-auto mt-8 p-6 bg-white shadow-md rounded-md">
  <h2 class="text-2xl font-semibold mb-4">Sign up</h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= render "users/shared/error_messages", resource: resource %>

    <div class="mb-4">
      <%= f.label :email, class: "block text-gray-700 font-bold mb-2" %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full rounded-md border border-gray-300 py-2 px-3" %>
    </div>

    <div class="mb-4">
      <%= f.label :password, class: "block text-gray-700 font-bold mb-2" %>
      <% if @minimum_password_length %>
      <em class="text-sm text-gray-600">(<%= @minimum_password_length %> characters minimum)</em>
      <% end %><br />
      <%= f.password_field :password, autocomplete: "new-password", class: "w-full rounded-md border border-gray-300 py-2 px-3" %>
    </div>

    <div class="mb-4">
      <%= f.label :password_confirmation, class: "block text-gray-700 font-bold mb-2" %><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "w-full rounded-md border border-gray-300 py-2 px-3" %>
    </div>

    <div class="mb-4">
      <%= f.label :address, class: "block text-gray-700 font-bold mb-2" %><br />
      <%= f.text_field :address, autocomplete: "email", class: "w-full rounded-md border border-gray-300 py-2 px-3" %>
    </div>

    <div class="mb-4">
      <%= f.label :city, class: "block text-gray-700 font-bold mb-2" %><br />
      <%= f.text_field :city, autocomplete: "email", class: "w-full rounded-md border border-gray-300 py-2 px-3" %>
    </div>

    <div class="mb-4">
      <%= f.label :province_id, class: "block text-gray-700 font-bold mb-2" %><br />
      <%= f.collection_select :province_id, Province.order(:name), :id, :name, { prompt: "Select a province" }, class: "w-full rounded-md border border-gray-300 py-2 px-3" %>
    </div>

    <div class="mb-4">
      <%= f.label :postal_code, class: "block text-gray-700 font-bold mb-2" %><br />
      <%= f.text_field :postal_code, autocomplete: "email", class: "w-full rounded-md border border-gray-300 py-2 px-3" %>
    </div>

    <div class="text-center">
      <%= f.submit "Sign up", class: "bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded" %>
    </div>
  <% end %>

  <%= render "users/shared/links" %>
</div>
